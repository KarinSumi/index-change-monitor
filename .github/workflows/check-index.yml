name: Check Index Changes

on:
  # รันทุกวันเวลา 22:00 UTC (5:00 น. เวลาไทย)
  schedule:
    - cron: '0 22 * * *'
  
  # สามารถรันด้วยตัวเองได้จากหน้า Actions
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run check script
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      run: python check_index.py
    
    - name: Commit and push if changed
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add previous_data.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "Update index data [skip ci]" && git push)
```

4. กด **"Commit changes..."**

---

## Step 6: เพิ่ม Discord Webhook ใน Secrets 🔐

**นี่คือส่วนสำคัญที่สุด!**

1. ไปที่แท็บ **"Settings"** ของ repo (บนสุด)
2. คลิกที่ **"Secrets and variables"** → **"Actions"** (เมนูซ้าย)
3. กด **"New repository secret"** (ปุ่มสีเขียว)
4. กรอกข้อมูล:
   - **Name:** `DISCORD_WEBHOOK`
   - **Secret:** `https://discord.com/api/webhooks/1453983739112722566/_-TZqiWqZkHObp9t2chSDDe8er6WhKVw9pzKGfyRlEc4XB...` (วาง Webhook URL ทั้งหมดของคุณ)
5. กด **"Add secret"**

✅ ตอนนี้ Webhook ของคุณปลอดภัยแล้ว! ไม่มีใครเห็นได้

---

## Step 7: ทดสอบรัน 🚀

1. ไปที่แท็บ **"Actions"** (บนสุด)
2. คลิกที่ **"Check Index Changes"** (ด้านซ้าย)
3. กด **"Run workflow"** (ปุ่มสีน้ำเงิน)
4. กด **"Run workflow"** อีกครั้ง (ใน dropdown)
5. รอสักครู่ (ประมาณ 1-2 นาที)

### ดูผลการรัน:
- คลิกที่ job ที่กำลังรัน (จะมีจุดสีเหลืองหมุน)
- คลิกที่ **"check"**
- คลิกที่ **"Run check script"** เพื่อดู log

### ถ้าสำเร็จ:
- ✅ จะเห็นสีเขียว
- 🔔 จะได้รับข้อความใน Discord!

---

## โครงสร้างไฟล์ทั้งหมด
```
index-monitor-bot/
├── .github/
│   └── workflows/
│       └── check-index.yml
├── check_index.py
├── requirements.txt
├── previous_data.json
└── README.md
```

---

## ตัวอย่างข้อความที่จะได้รับใน Discord

**ครั้งแรก:**
```
🚀 เริ่มต้นติดตาม Index Changes

📊 S&P 500: 503 หุ้น
📊 Nasdaq-100: 100 หุ้น

✅ Bot พร้อมทำงานแล้ว! จะตรวจสอบทุกวันเวลา 05:00 น.
```

**เมื่อมีการเปลี่ยนแปลง:**
```
🔔 S&P 500 มีการเปลี่ยนแปลง!
📅 วันที่: 2024-12-26 22:05:30

✅ เพิ่มเข้า S&P 500:
  • NVDA
  • TSLA

❌ ถอดออกจาก S&P 500:
  • XYZ

📊 จำนวนปัจจุบัน: 503 หุ้น
